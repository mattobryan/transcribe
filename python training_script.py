
4. Evaluate the model using the evaluation metrics

## Configuration

All configuration parameters are defined in `config.yaml`. Key settings include:

- Audio processing parameters (sample rate, MFCC features)
- Model architecture (hidden dimensions, layers, dropout)
- Training parameters (batch size, learning rate, epochs)

## License

[Add your license information here]
