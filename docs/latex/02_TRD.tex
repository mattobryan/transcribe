\documentclass[11pt]{article}
\usepackage{kctdocs}
\begin{document}
\kcttitle{02}{Technical Requirements Document (TRD)}{%
Status & Draft \\
Version & 1.2 \textperiodcentered\ 2026-09-24 22:26 \textperiodcentered\ \hyperref[changelog]{change log} \\
Related & \href{01_PRD.pdf}{01 PRD}, \href{04_APP_FLOW.pdf}{04 App Flow}, \href{05_BACKEND_SCHEMA.pdf}{05 Backend Schema} \\
}
{\small\setlength{\parskip}{0pt}\setcounter{tocdepth}{1}\tableofcontents}
\bigskip
\hypertarget{architecture-overview}{%
\section{Architecture overview}\label{architecture-overview}}

\begin{figure}[!htbp]\centering
\resizebox{\ifdim\width>\linewidth\linewidth\else\width\fi}{!}{\input{figures/architecture}}
\caption{System architecture: local CPU services and Kaggle GPU training.}
\end{figure}

Principles:

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  \textbf{Human text is authoritative.} Model output is stored as a \emph{hypothesis} next to reviewer text and is never written over it.
\item
  \textbf{Immutability.} Source audio and transcripts are content-addressed (sha256) and never modified. Datasets are frozen snapshots, and model versions point to exactly one snapshot.
\item
  \textbf{One process model.} A single user on CPU needs no Redis or Celery. A worker process polls a jobs table in SQLite.
\item
  \textbf{Engines behind interfaces.} Aligner, VAD and ASR are swappable: a Protocol plus a registry, so the CTC comparison model or a larger Whisper plugs in without UI changes.
\end{enumerate}

\hypertarget{model-strategy}{%
\section{Model strategy}\label{model-strategy}}

\hypertarget{why-not-a-proper-english-model--a-proper-swahili-model-blended}{%
\subsection{Why not ``a proper English model + a proper Swahili model, blended''}\label{why-not-a-proper-english-model--a-proper-swahili-model-blended}}

This was the proposal in the requirements discussion. It does not work for Kenyan code-switching, for three reasons:

\begin{itemize}
\tightlist
\item
  \textbf{Routing needs per-word language ID before transcription.} A router (language ID → English or Swahili model) decides per segment. Switches happen mid-sentence and \emph{mid-word} (\emph{nimedownload}), below any practical language-ID granularity. Every routing error becomes a transcription error.
\item
  \textbf{Monolingual models cannot output the other language.} A Swahili model spells English words in Swahili orthography (\emph{daunlodi}), and an English model maps Swahili words onto English lookalikes. Merging hypotheses (ROVER-style) needs both hypotheses to contain the right word somewhere, and for intra-word switches neither does.
\item
  \textbf{Cost and complexity.} It needs twice the compute on CPU, two fine-tuning tracks, and a merge heuristic with no training signal.
\end{itemize}

\textbf{What we do instead:} one multilingual pretrained model whose pretraining already covered both languages, fine-tuned on Kenyan code-switched speech. The blending happens in two places where it works:

\begin{itemize}
\tightlist
\item
  \textbf{The data mixture.} In-domain code-switched segments, plus public Kiswahili and public Kenyan-accented English, blended by sampling weights. This teaches the switch and keeps both monolingual abilities.
\item
  \textbf{The tokenizer.} Whisper\textquotesingle s byte-level BPE can emit any Latin string, so \emph{nime} + \emph{download} is just a token sequence. Nothing forces a single language per word.
\end{itemize}

\hypertarget{chosen-models}{%
\subsection{Chosen models}\label{chosen-models}}
\kctchanged{1.2}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.130}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.435}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.435}}@{}}
\toprule\noalign{}
Role & Model & Why \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\textbf{v1 production} & \texttt{openai/\allowbreak{}whisper-\allowbreak{}small} (244M), full fine-tune & Fits a Kaggle T4 in fp16. Readable output with casing and punctuation. Its 30 s window matches our segments. Mature CPU serving via faster-whisper int8. The fast iteration suits learning the process \\
v2 candidate & \texttt{openai/\allowbreak{}whisper-\allowbreak{}large-\allowbreak{}v3-\allowbreak{}turbo} (809M, 4-layer decoder), LoRA or 8-bit optimiser & Much stronger encoder with an inference cost close to small/medium. Adopted only if it beats v1 on the frozen dev set by a meaningful margin \\
Comparison (learning track) & \texttt{facebook/\allowbreak{}w2v-\allowbreak{}bert-\allowbreak{}2.\allowbreak{}0} + CTC head, optional KenLM trained on code-switched text & Contrasting paradigm: no hallucination, faithful verbatim output, character-level. Teaches CTC fine-tuning and LM fusion. Not production because it has no punctuation and weaker CPU tooling \\
Aligner (not trained) & MMS-300m forced-alignment CTC model (\texttt{MahmoudAshraf/\allowbreak{}mms-\allowbreak{}300m-\allowbreak{}1130-\allowbreak{}forced-\allowbreak{}aligner}) loaded with Hugging Face \texttt{transformers}; the Viterbi alignment is our own NumPy code (\texttt{pilot/\allowbreak{}ctc\_\allowbreak{}align.\allowbreak{}py}). Latin EN/SW text needs no uroman: NFKD normalisation strips diacritics & Language-agnostic: it aligns any romanised text, so mixed EN/SW transcripts align without choosing a language. Owning the Viterbi removes the torchaudio deprecation risk and lets us add the two-pass anchoring of §\ref{long-form-alignment} \\
VAD & Silero VAD & Robust on noise and music compared with the current energy threshold. Runs on CPU in ms per window \\
Legacy & Existing MFCC CNN-LSTM CTC/Seq2Seq & Kept under \texttt{legacy/\allowbreak{}} as an educational baseline only. Not deployable (see §2.4) \\
\end{longtable}

Uncertainty on the aligner: torchaudio has been deprecating parts of its API. The aligner sits behind an interface and its dependency is pinned, so either backend can be used.

\hypertarget{whisper-fine-tuning-specifics}{%
\subsection{Whisper fine-tuning specifics}\label{whisper-fine-tuning-specifics}}

\begin{itemize}
\tightlist
\item
  \textbf{Language token:} fixed \texttt{<|\allowbreak{}sw|\allowbreak{}>} + \texttt{<|\allowbreak{}transcribe|\allowbreak{}>} + \texttt{<|\allowbreak{}notimestamps|\allowbreak{}>} for all training samples. Kenyan code-switching is predominantly Kiswahili-framed, and a fixed token removes the language-ID step at inference. \textbf{Experiment E3} compares \texttt{sw} vs \texttt{en} vs no language token on the dev set. This is a cheap and instructive ablation.
\item
  \textbf{Non-speech examples:} about 5\% of training samples are music/noise/silence windows with an empty target. This is the most direct fix for church and TV hallucination.
\item
  \textbf{Data mixture (v1):} in-domain approved segments weight 1.0. Public Kiswahili (Common Voice sw, FLEURS sw\_ke) weight about 0.3, to prevent forgetting and add speaker diversity. Kenyan-accented English (AfriSpeech-200 Kenyan subset) weight about 0.2. Licences must be checked before any commercial use (AfriSpeech is non-commercial).
\item
  \textbf{Hyperparameters (starting point, T4 16 GB):} fp16, lr 1e-5, warmup 500, per-device batch 16 with gradient checkpointing, effective batch 32, 3--5k steps, evaluate every 500 steps on dev WER, keep the best checkpoint. SpecAugment is on (\texttt{mask\_\allowbreak{}time\_\allowbreak{}prob=\allowbreak{}0.\allowbreak{}05}).
\item
  \textbf{Kaggle constraints:} a weekly GPU quota (\textasciitilde30 h) and a hard per-session limit, so we save checkpoints to \texttt{/\allowbreak{}kaggle/\allowbreak{}working} every 500 steps, the training script supports \texttt{resume\_\allowbreak{}from\_\allowbreak{}checkpoint}, and one session does one experiment.
\end{itemize}

\hypertarget{why-the-legacy-model-cannot-be-the-product}{%
\subsection{Why the legacy model cannot be the product}\label{why-the-legacy-model-cannot-be-the-product}}

\begin{itemize}
\tightlist
\item
  Hop 512 at 16 kHz gives 32 ms frames, and the three pooling layers make that 256 ms per CTC output frame. A 30 s segment has about 117 frames against about 400 target characters, and CTC requires frames ≥ target length. \texttt{CTCLoss(zero\_\allowbreak{}infinity=\allowbreak{}True)} silently zeroes these losses.
\item
  The tokenizer removes spaces, so output cannot be readable.
\item
  It has no pretraining, and hundreds of hours of audio are needed before from-scratch ASR becomes competitive with a fine-tuned pretrained model.
\end{itemize}

If kept for learning, fix it with hop 160 (10 ms), 2× downsampling, a space token, and no SOS/EOS in CTC targets.

\hypertarget{segment-length-policy}{%
\section{Segment length policy}\label{segment-length-policy}}

(Answers ``what chunk length?'')

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.106}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.453}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.441}}@{}}
\toprule\noalign{}
Parameter & Value & Reason \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Max segment & 30.0 s & Whisper\textquotesingle s encoder window is exactly 30 s; anything longer is truncated \\
Target & 8--20 s & Enough context for the decoder; still fits CTC memory for the comparison model \\
Min segment & 1.5 s & Shorter segments carry little context and add label noise \\
Split points & Aligned inter-word gaps ≥ 300 ms; prefer the largest gap; prefer turn boundaries & Never cut a word; natural prosodic units \\
Padding & 100 ms on each side, clipped to the neighbouring word midpoint & Avoids clipped onsets without leaking neighbouring words \\
Speaker & A segment never spans a speaker turn change unless the turn is \textless{} 1.5 s & Clean targets; overlap is flagged \\
\end{longtable}

At inference, Silero VAD speech regions are merged into windows of ≤ 30 s. Windows are cut at the longest silence and overlap by 0 s (VAD-cut, so no stitching is needed).

\hypertarget{corpus-pipeline}{%
\section{Corpus pipeline}\label{corpus-pipeline}}

\begin{lstlisting}
ingest → normalise audio → parse transcript → normalise text for alignment
→ long-form alignment → word timestamps + scores → segmentation
→ auto-flags + priority score → review → approve → dataset snapshot
\end{lstlisting}

\hypertarget{ingest}{%
\subsection{Ingest}\label{ingest}}

\begin{itemize}
\tightlist
\item
  ffmpeg decodes to a 16 kHz mono 16-bit WAV. The original file is kept byte-identical, stored at \texttt{data/\allowbreak{}store/\allowbreak{}originals/\allowbreak{}<sha256>.\allowbreak{}<ext>}.
\item
  The transcript is parsed by the existing \texttt{docx\_\allowbreak{}parser} / \texttt{pdf\_\allowbreak{}parser}. The speaker regex and language hints (italic → \texttt{sw}, a convention that is verified per source) are reused.
\end{itemize}

\hypertarget{text-normalisation-for-alignment-a-separate-text-never-shown-as-the-label}{%
\subsection{Text normalisation for alignment (a separate text, never shown as the label)}\label{text-normalisation-for-alignment-a-separate-text-never-shown-as-the-label}}

\kctchanged{1.2}
\begin{itemize}
\tightlist
\item
  Remove transcriber markers and record them on the neighbouring word. Real transcripts use several forms, all recognised: \texttt{[Unclear 13:10-13:15]}, \texttt{[No speech - activity, 17:22-17:24]}, \texttt{[[Unclear 1:16:07]]} and \texttt{(Unclear: 1:16:07-1:16:08]]}. Their \textbf{timestamps are kept} and used as alignment boundaries and as an accuracy check (§\ref{long-form-alignment}).
\item
  Participant references such as \texttt{P7.} stand in for a spoken name: they are not aligned, and the segment is flagged \texttt{redacted\_\allowbreak{}marker} (excluded from training).
\item
  False starts and cut-offs written with \texttt{--}, an em dash or an ellipsis (\texttt{tuta--tutaanza}) are split into separate words. Punctuation-only tokens stay attached to the neighbouring word.
\item
  Lowercase and strip diacritics (NFKD); keep only letters in the aligner's vocabulary.
\item
  \textbf{Numbers (current pilot):} digits are not aligned; their words get times interpolated from neighbours, and the segment is flagged \texttt{has\_\allowbreak{}number}. \textbf{Planned (Phase 1):} verbalisation per \href{STYLE_GUIDE.pdf}{STYLE\_GUIDE §4}, generating an English and a Kiswahili candidate and keeping the one the aligner scores higher; near ties flag \texttt{number\_\allowbreak{}check}. English via \texttt{num2words}, Kiswahili via a rule-based \texttt{corpus/\allowbreak{}numbers\_\allowbreak{}sw.\allowbreak{}py}.
\item
  Optional display ITN (words → digits) runs on Transcribe output only (P1), never on training text.
\item
  Each word keeps its original spelling, which becomes the segment label; the aligner only sees the normalised letters.
\end{itemize}

\hypertarget{long-form-alignment}{%
\subsection{Long-form alignment}\label{long-form-alignment}}

\kctchanged{1.2}
Validated on NRCCW\_KSM09 (2~h~05~min, 1\,237 turns, 12\,305 words); see the \hyperref[changelog]{change log} for the measurements.
\begin{itemize}
\tightlist
\item
  \textbf{Emissions.} CTC log-probabilities for the whole recording in 30~s windows with 2~s context, stitched and cached (\texttt{emissions.\allowbreak{}npy}, about 23~MB for 2~h). This is the only slow step: about 0.2$\times$ real time on a 4-core CPU, 0.8$\times$ on a slower laptop, a few minutes on a Kaggle GPU.
\item
  \textbf{Confidence} is \emph{relative}: for each letter, how close the forced path is to the aligner's best guess, measured only on frames where the path emits a letter or the model hears one. Silence agreement proves nothing and is ignored. Spoken text scores near 1 even in noisy far-field audio; text that was never spoken scores low (FLEURS mock: spoken 0.70--1.00, unspoken 0.41). The earlier absolute-posterior measure rejected most real turns on noisy audio.
\item
  \textbf{Pass 1, anchors.} Each turn of at least 12 letters is searched in a window after the last trusted turn. It becomes an anchor only if its confidence clears a length-dependent bar (0.8 for short turns, easing to 0.5 for long ones), because short text can match by chance. Shorter turns (``Mm-hmm'') are never anchors.
\item
  \textbf{Pass 2, fill.} Everything between two consecutive anchors is aligned as one block, bounded on both sides, so it cannot drift and short turns are placed in context. The block is split at timestamped markers: words before a marker must end near its start and words after it must start near its end (±3~s tolerance). Turns with no room between their anchors (usually backchannels spoken over the other person) stay \emph{unfilled}.
\item
  \textbf{Safety.} Timings that run backwards are dropped. Words whose confidence is below 0.5 make their segment \texttt{low\_\allowbreak{}alignment}, which keeps it out of training. On NRCCW\_KSM09 every word placed more than 10~s from a transcriber timestamp fell in such a segment.
\item
  \textbf{Accuracy check.} The report compares aligned times with the transcriber's timestamps (median error, share within 3~s and 10~s). Tuning used half of the markers as boundaries and scored on the other half.
\item
  Output per word: \texttt{start}, \texttt{end}, \texttt{score} (relative confidence, 0--1).
\end{itemize}

\hypertarget{priority-score-for-the-review-queue}{%
\subsection{Priority score for the review queue}\label{priority-score-for-the-review-queue}}

\begin{lstlisting}
priority = 0.5·(1 − alignment_conf) + 0.2·has_unclear_marker + 0.1·overlap_marker
         + 0.2·model_disagreement (WER(aligned_text, model_hypothesis), once a model exists)
\end{lstlisting}

The review queue sorts by \texttt{priority} descending. Segments with high confidence and low disagreement can be \textbf{batch-approved after spot-checking} a random 10\%, controlled by a setting and off by default.

\hypertarget{recordings-without-transcripts}{%
\subsection{Recordings without transcripts}\label{recordings-without-transcripts}}

Silero VAD splits them into ≤ 30 s chunks, the current production model pre-fills the text (stored as a hypothesis, with the reviewer text starting from a copy), and they go into the same review UI.

\hypertarget{inference-pipeline}{%
\section{Inference pipeline}\label{inference-pipeline}}

\begin{enumerate}
\def\labelenumi{\arabic{enumi}.}
\tightlist
\item
  Decode with ffmpeg → 16 kHz mono float32.
\item
  Silero VAD (threshold 0.5, min speech 250 ms, min silence 400 ms) gives speech regions.
\item
  Merge regions into windows of ≤ 30 s.
\item
  Run faster-whisper on each window: \texttt{language=\allowbreak{}"sw"}, \texttt{task=\allowbreak{}"transcribe"}, \texttt{beam\_\allowbreak{}size=\allowbreak{}5}, \texttt{condition\_\allowbreak{}on\_\allowbreak{}previous\_\allowbreak{}text=\allowbreak{}False}, \texttt{word\_\allowbreak{}timestamps=\allowbreak{}True}, \texttt{vad\_\allowbreak{}filter=\allowbreak{}False} (VAD has already been applied).
\item
  Guards: drop segments with \texttt{no\_\allowbreak{}speech\_\allowbreak{}prob > 0.\allowbreak{}6} and \texttt{avg\_\allowbreak{}logprob < −1.\allowbreak{}0}. Drop segments with compression ratio \textgreater{} 2.4 (repetition loops). Collapse a 3-gram repeated more than 3 times.
\item
  Post-processing: timestamps are offset back to absolute time, and segments are persisted as \texttt{job\_\allowbreak{}segments}.
\item
  Export renderers: TXT, SRT, VTT, JSON, DOCX.
\end{enumerate}

Batching: windows run sequentially, with \texttt{cpu\_\allowbreak{}threads =\allowbreak{} physical cores} and \texttt{compute\_\allowbreak{}type=\allowbreak{}"int8"}. Progress = processed audio seconds / total speech seconds.

\hypertarget{service-design}{%
\section{Service design}\label{service-design}}

\hypertarget{processes}{%
\subsection{Processes}\label{processes}}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.104}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.207}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.689}}@{}}
\toprule\noalign{}
Process & Command & Responsibility \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
\texttt{api} & \texttt{python -\allowbreak{}m transcribe.\allowbreak{}api} & FastAPI + Gradio mounted at \texttt{/\allowbreak{}}. Handles CRUD and enqueues jobs \\
\texttt{worker} & \texttt{python -\allowbreak{}m transcribe.\allowbreak{}worker} & Polls \texttt{jobs} (\texttt{status=\allowbreak{}'queued'}), runs alignment, transcription, evaluation and export, and heartbeats \\
\end{longtable}

Both processes share SQLite in WAL mode. A job is claimed with \texttt{UPDATE jobs SET status=\allowbreak{}'running',\allowbreak{} worker\_\allowbreak{}id=\allowbreak{}? WHERE id=\allowbreak{}? AND status=\allowbreak{}'queued'}. A stale heartbeat (\textgreater{} 2 min) puts the job back in the queue.

\hypertarget{rest-api-apiv1}{%
\subsection{\texorpdfstring{REST API (\texttt{/api/v1})}{REST API (/api/v1)}}\label{rest-api-apiv1}}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.103}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.271}}>{\raggedright\arraybackslash}p{(\linewidth - 6\tabcolsep) * \real{0.626}}@{}}
\toprule\noalign{}
Method & Path & Purpose \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
POST & \texttt{/\allowbreak{}recordings} & Multipart upload of audio (+ transcript), domain, consent\_scope \\
GET & \texttt{/\allowbreak{}recordings}, \texttt{/\allowbreak{}recordings/\allowbreak{}\{id\}} & List and detail, with review progress \\
POST & \texttt{/\allowbreak{}recordings/\allowbreak{}\{id\}/\allowbreak{}align} & Enqueue alignment + segmentation \\
GET & \texttt{/\allowbreak{}review/\allowbreak{}queue?recording\_\allowbreak{}id=\allowbreak{}\&limit=\allowbreak{}} & Next segments by priority \\
GET & \texttt{/\allowbreak{}segments/\allowbreak{}\{id\}} & Segment + current revision + hypotheses + audio URL \\
POST & \texttt{/\allowbreak{}segments/\allowbreak{}\{id\}/\allowbreak{}revisions} & Create a new revision (text, bounds, flags, spans, status). Rejected with 409 on version conflict \\
POST & \texttt{/\allowbreak{}segments/\allowbreak{}\{id\}/\allowbreak{}hypothesis} & Run the model on the segment (stored as a hypothesis only) \\
GET & \texttt{/\allowbreak{}segments/\allowbreak{}\{id\}/\allowbreak{}audio} & WAV slice (rendered on demand from the recording, cached) \\
POST & \texttt{/\allowbreak{}datasets} & Build a snapshot from a filter spec + split spec \\
POST & \texttt{/\allowbreak{}datasets/\allowbreak{}\{id\}/\allowbreak{}export} & Write the HF-format package for Kaggle \\
POST & \texttt{/\allowbreak{}models} & Register a model (upload or path), enqueue CT2 conversion \\
POST & \texttt{/\allowbreak{}models/\allowbreak{}\{id\}/\allowbreak{}promote} & Set the production model \\
POST & \texttt{/\allowbreak{}evaluations} & Enqueue evaluation of a model on a dataset split \\
GET & \texttt{/\allowbreak{}evaluations/\allowbreak{}\{id\}} & Metrics + per-segment results \\
POST & \texttt{/\allowbreak{}transcriptions} & Upload a file for transcription (enqueue a job) \\
GET & \texttt{/\allowbreak{}transcriptions/\allowbreak{}\{id\}} & Status, progress, segments \\
PATCH & \texttt{/\allowbreak{}transcriptions/\allowbreak{}\{id\}/\allowbreak{}segments/\allowbreak{}\{seg\_\allowbreak{}id\}} & Save an edit \\
GET & \texttt{/\allowbreak{}transcriptions/\allowbreak{}\{id\}/\allowbreak{}export?format=\allowbreak{}srt} & Download \\
POST & \texttt{/\allowbreak{}transcriptions/\allowbreak{}\{id\}/\allowbreak{}promote} & Copy edited segments into the corpus (consent-checked) \\
\end{longtable}

Optimistic concurrency: every revision POST carries \texttt{base\_\allowbreak{}revision\_\allowbreak{}id}. A mismatch returns 409, so a second browser tab cannot overwrite silently.

\hypertarget{package-layout-target}{%
\subsection{Package layout (target)}\label{package-layout-target}}

\begin{lstlisting}
src/transcribe/
  api/            # FastAPI app, routers, schemas (pydantic)
  worker/         # job loop, handlers
  db/             # SQLAlchemy models, session, Alembic migrations
  corpus/
    ingest.py  parsers/ (docx_parser.py, pdf_parser.py — moved from preprocessing)
    textnorm.py  align.py  segment.py  priority.py
  inference/
    vad.py  engine.py (Protocol + faster-whisper impl)  longform.py  guards.py  export/
  datasets/       # snapshot build, grouped split, HF export
  finetune/       # whisper_train.py, w2vbert_ctc_train.py (run inside Kaggle)
  evaluation/     # normalizer.py, metrics.py (WER/CER/switch-point/hallucination), report.py
  ui/             # Gradio Blocks: transcribe.py, review.py, models.py, theme.py
  legacy/         # ctc_model.py, seq2seq.py, trainer.py, data/ (unchanged behaviour)
notebooks/kaggle/ # thin notebooks calling transcribe.finetune.*
tests/
\end{lstlisting}

The package imports as \texttt{transcribe} (installed via \texttt{pip install -\allowbreak{}e .\allowbreak{}}). The \texttt{sys.\allowbreak{}path} / \texttt{src.\allowbreak{}} import hack is removed.

\hypertarget{dependencies}{%
\section{Dependencies}\label{dependencies}}
\kctchanged{1.2}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.196}}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.804}}@{}}
\toprule\noalign{}
Group & Packages \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
core & fastapi, uvicorn, sqlalchemy≥2, alembic, pydantic≥2, python-multipart, soundfile, numpy, ffmpeg (system) \\
corpus & python-docx, pypdf, transformers\,<\,5 (aligner model), torch; librosa + soundfile for audio \\
pilot (now) & \texttt{requirements-\allowbreak{}pilot.\allowbreak{}txt}: the annotation requirements plus transformers, faster-whisper, jiwer \\
inference & faster-whisper, silero-vad (onnx), python-docx (export) \\
ui & gradio≥4 \\
eval & jiwer \\
finetune (Kaggle only) & transformers, datasets, accelerate, evaluate, peft (v2), bitsandbytes (v2) \\
legacy & librosa, scikit-learn, editdistance, nltk \\
\end{longtable}

Requirements are split into \texttt{requirements/\allowbreak{}\{core,\allowbreak{}corpus,\allowbreak{}inference,\allowbreak{}ui,\allowbreak{}finetune,\allowbreak{}legacy\}.\allowbreak{}txt}. Versions are pinned with upper bounds, following the current practice.

\hypertarget{evaluation}{%
\section{Evaluation}\label{evaluation}}

\hypertarget{splits-answers-how-should-we-split}{%
\subsection{Splits (answers ``how should we split?'')}\label{splits-answers-how-should-we-split}}
\kctchanged{1.1}

\begin{itemize}
\tightlist
\item
  \textbf{Group by recording, never by chunk.} Chunks from one recording share speaker, microphone, room and topic, so a random chunk split leaks all of them and inflates results.
\item
  If a speaker appears in several recordings (the same interviewer), group by speaker cluster: all recordings of that speaker go to the same split, or that speaker is held out explicitly.
\item
  \textbf{Size dev/test in hours and recordings, not percentages.} Evaluation sets only need to be big enough for a stable WER, and variance comes from speakers and recordings more than from word count. Long-run target: \textbf{test ≈ 3 h from ≥ 15 recordings, dev ≈ 2--3 h from ≥ 10 recordings}. Everything else is train; as audio keeps arriving, train grows and dev/test stay fixed.
\item
  \textbf{Current corpus (v1): 11 recordings, 8 h 56 min.} test\_v1 = 2 recordings (≈ 1.5 h, 17\%), dev\_v1 = 1 recording (≈ 50 min, 10\%), train = 8 recordings (≈ 6.5 h, 73\%), including the 2 h 05 min recording. The test set is larger than dev because it is the number every model version is judged on; dev only has to rank checkpoints. With so few recordings a single test pair is noisy, so final v1 numbers are also reported as leave-two-recordings-out cross-validation over 3 folds (3 Kaggle runs), and the frozen split is kept for version-to-version comparison.
\item
  \textbf{Shared speakers:} recordings are mostly of different people, but some people may appear in more than one recording. Recordings that share any person (interviewer or respondent) form one \emph{speaker group}, and a whole group always goes to the same split. Groups come from the file-naming convention, cross-checked by a speaker-embedding similarity pass (ECAPA-TDNN embeddings per recording, cosine similarity above a tuned threshold flags a probable shared speaker for manual confirmation). If an interviewer still ends up in several splits, reports add a \emph{respondent-only} WER.
\item
  \textbf{Test is frozen forever} (for comparability across model versions), stratified so it contains clean and noisy, low and high code-switch density, and each domain available. Dev is used for checkpoint selection and ablations.
\item
  \textbf{Audio still arriving:} fix \texttt{test\_\allowbreak{}v1} and \texttt{dev\_\allowbreak{}v1} from the recordings available at the end of Phase 2, and never change them. New recordings go to train by default. A new domain or a clearly new condition (TV, church, a new region) first contributes ≈ 1 h to an \texttt{external\_\allowbreak{}<domain>} test set.
\item
  New recordings go to train by default. New \emph{domains} (TV, church) first get a held-out evaluation set of ≥ 1 h before any of their data goes into train.
\item
  Also keep FLEURS \texttt{sw\_\allowbreak{}ke} test as a forgetting check. Test-set segments are never used for anything except final reports.
\end{itemize}

\hypertarget{normaliser-versioned-normalizer_version-stored-with-each-evaluation}{%
\subsection{\texorpdfstring{Normaliser (versioned, \texttt{normalizer\_version} stored with each evaluation)}{Normaliser (versioned, normalizer\_version stored with each evaluation)}}\label{normaliser-versioned-normalizer_version-stored-with-each-evaluation}}

Lowercase. Strip punctuation except intra-word apostrophes. Remove hyphens (so \emph{nime-download} ≡ \emph{nimedownload}). Collapse whitespace. Remove bracketed annotations. Map a small table of spelling variants (e.g. \emph{ok/okay}) from the style guide. Verbalise any remaining digits with the same rules as the corpus pipeline, so digit and word outputs score the same.

\hypertarget{metrics}{%
\subsection{Metrics}\label{metrics}}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.204}}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.796}}@{}}
\toprule\noalign{}
Metric & Definition \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
WER / CER & jiwer on normalised text, micro-averaged (total errors / total reference words) \\
Per-language WER & Using reference language spans: errors attributed to \texttt{en}, \texttt{sw} and \texttt{mixed} reference words via the jiwer alignment \\
Switch-point error rate & Over reference words adjacent to a language change (plus all \texttt{mixed} words): the fraction misrecognised. This directly measures the code-switching capability \\
Hallucination rate & Words emitted per minute on reference non-speech segments (the non-speech test subset) \\
RTF & Wall time / audio duration on the reference CPU \\
\end{longtable}

Reports are broken down by domain and by flag (clean / noisy / overlap).

\hypertarget{security-and-privacy}{%
\section{Security and privacy}\label{security-and-privacy}}

\begin{itemize}
\tightlist
\item
  The app binds to \texttt{127.\allowbreak{}0.\allowbreak{}0.\allowbreak{}1} by default. Exposing it on a network requires setting \texttt{TRANSCRIBE\_\allowbreak{}AUTH\_\allowbreak{}TOKEN} (a bearer token middleware) in v1.
\item
  \texttt{consent\_\allowbreak{}scope ∈ \{training,\allowbreak{} eval\_\allowbreak{}only,\allowbreak{} transcription\_\allowbreak{}only\}} on every recording is enforced in the dataset builder (training split only from \texttt{training}), in the Kaggle export, and in ``promote to corpus''.
\item
  Kaggle datasets are created private. The export package contains only audio slices and text, never names from the transcript header or file paths.
\item
  No demographic or ethnicity fields are stored. Speaker labels are opaque (\texttt{S1}, \texttt{interviewer}).
\item
  Uploaded transcription files are deleted after N days (configurable) unless promoted.
\end{itemize}

\hypertarget{testing-strategy}{%
\section{Testing strategy}\label{testing-strategy}}
\kctchanged{1.2}

\begin{longtable}[]{@{}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.110}}>{\raggedright\arraybackslash}p{(\linewidth - 4\tabcolsep) * \real{0.890}}@{}}
\toprule\noalign{}
Level & What \\
\midrule\noalign{}
\endhead
\bottomrule\noalign{}
\endlastfoot
Unit & Text normaliser, segmenter on synthetic word timings, priority, grouped split (asserts no recording in two splits), metric functions (hand-computed cases incl. switch points), guards \\
Integration & A 60 s synthetic fixture (TTS or public-domain clip + transcript) goes through align → segment → dataset export, all on CPU in \textless{} 2 min \\
Pilot (now) & 24 tests in \texttt{tests/\allowbreak{}pilot}: text preparation on real transcript quirks, the Viterbi, segmentation, metrics, file pairing, a 200-turn alignment regression with unspoken turns, and an end-to-end run with a fake aligner and fake Whisper \\
Smoke test & \texttt{pilot.\allowbreak{}make\_\allowbreak{}mock}: a 4-minute interview stitched from public FLEURS Kiswahili and English clips, with one written-but-unspoken turn that must be the only one flagged \\
API & FastAPI TestClient: revision conflict (409), consent enforcement, job lifecycle \\
Model & The Kaggle notebook prints dev WER. \texttt{evaluate} reproduces the same WER locally within ±0.5 absolute \\
CI & GitHub Actions: ruff + pytest (unit + integration with a tiny aligner mock) on Ubuntu, Python 3.10 \\
\end{longtable}

\kctchangelog
\begin{kctversions}
1.0 & 2026-09-24 00:36 & \texttt{053e09d} & First LaTeX version (converted from the Markdown draft of 2026-09-23). \\
1.1 & 2026-09-24 01:53 & \texttt{695b407}, \texttt{8a3be55} & Splits sized for the real corpus (11 recordings, 8~h~56~min); split by speaker group. \\
1.2 & 2026-09-24 22:26 & this commit & Pilot results: aligner implementation, text preparation, two-pass long-form alignment, dependencies, tests. \\
\end{kctversions}

\kctchange{1.2}{§2.2 Chosen models: aligner row}{chosen-models}
 {\kctsrc{8a3be55a33b1a843a27d87795a74872523d940bb}{02_TRD.tex}{74}{74}{v1.1 source, line 74} \textperiodcentered\ \href{archive/v1.1/02_TRD.pdf}{v1.1 PDF}}
 {``MMS-300m forced-alignment model + uroman romaniser (via \texttt{ctc-forced-aligner}, or torchaudio MMS\_FA pinned).''}
 {The same MMS-300m aligner, loaded with \texttt{transformers}; our own NumPy Viterbi; no uroman (NFKD normalisation). Removes the torchaudio deprecation risk and enables two-pass anchoring.}

\kctchange{1.2}{§4.2 Text normalisation for alignment}{text-normalisation-for-alignment-a-separate-text-never-shown-as-the-label}
 {\kctsrc{8a3be55a33b1a843a27d87795a74872523d940bb}{02_TRD.tex}{156}{171}{v1.1 source, lines 156--171} \textperiodcentered\ \href{archive/v1.1/02_TRD.pdf}{v1.1 PDF}}
 {Remove bracketed annotations; romanise with uroman; verbalise numbers with English/Kiswahili candidates chosen by the aligner; keep a word-index map.}
 {Recognises the marker forms found in the real transcript and \textbf{keeps their timestamps}; treats \texttt{P7}-style participant references as redactions; splits false starts (\texttt{tuta--tutaanza}); records that number verbalisation is still planned and the pilot interpolates digits and flags \texttt{has\_number}.}

\kctchange{1.2}{§4.3 Long-form alignment}{long-form-alignment}
 {\kctsrc{8a3be55a33b1a843a27d87795a74872523d940bb}{02_TRD.tex}{173}{186}{v1.1 source, lines 173--186} \textperiodcentered\ \href{archive/v1.1/02_TRD.pdf}{v1.1 PDF}}
 {``Alignment is turn-anchored: each transcript turn is aligned inside a search window around its expected position (moving anchor)\ldots{} score (mean frame log-posterior\ldots) A turn whose mean confidence is below θ (initially 0.5) is flagged \texttt{low\_alignment}.''}
 {Relative confidence on informative frames; pass 1 trusts only confident anchor turns (length-dependent bar); pass 2 aligns each block between anchors, split at transcript timestamps; backwards timings dropped. On NRCCW\_KSM09: words with timings 6\,643 → 11\,590 of 12\,305; segment hours 1.04 → 1.70; clean hours 0.82 → 0.91; no word over 30~s from a transcriber timestamp (was 2 of 14 placed); held-out median error 1.3~s.}

\kctchange{1.2}{§7 Dependencies}{dependencies}
 {\kctsrc{8a3be55a33b1a843a27d87795a74872523d940bb}{02_TRD.tex}{312}{318}{v1.1 source, lines 312--318} \textperiodcentered\ \href{archive/v1.1/02_TRD.pdf}{v1.1 PDF}}
 {Corpus group: ``python-docx, pypdf, ctc-forced-aligner (or torchaudio pinned), uroman, torch (CPU)''.}
 {Corpus group uses \texttt{transformers}; new \emph{pilot} row for \texttt{requirements-pilot.txt} (transformers, faster-whisper, jiwer).}

\kctchange{1.2}{§10 Testing strategy}{testing-strategy}
 {\kctsrc{8a3be55a33b1a843a27d87795a74872523d940bb}{02_TRD.tex}{402}{407}{v1.1 source, lines 402--407} \textperiodcentered\ \href{archive/v1.1/02_TRD.pdf}{v1.1 PDF}}
 {Unit, integration, API, model and CI rows only (all planned).}
 {Adds what exists now: the 24 pilot tests and the FLEURS mock-interview smoke test.}

\kctchange{1.1}{§8.1 Splits}{splits-answers-how-should-we-split}
 {\kctsrc{053e09d4042e52bc8dfa98423c13d99a42a60109}{02_TRD.tex}{334}{341}{v1.0 source, lines 334--341} \textperiodcentered\ \href{archive/v1.0/02_TRD.pdf}{v1.0 PDF}}
 {``Target: test ≈ 3 h from ≥ 15 recordings, dev ≈ 2--3 h from ≥ 10 recordings. Everything else is train. With 40 h that is about 85/7/8.''}
 {Keeps that as the long-run target and adds the v1 split for the real corpus (11 recordings, 8~h~56~min): test 2 recordings (≈~1.5~h), dev 1 (≈~50~min), train 8 (≈~6.5~h), with 3-fold leave-two-recordings-out cross-validation. Recordings sharing a person form a \emph{speaker group} that never spans splits (naming convention plus an ECAPA speaker-embedding check).}
\end{document}
