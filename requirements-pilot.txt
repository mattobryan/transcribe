# Single-recording pilot: forced alignment + zero-shot Whisper evaluation.
#   python -m pip install -r requirements-pilot.txt
-r requirements-annotation.txt
transformers>=4.40,<5      # MMS-300m CTC aligner (AutoModelForCTC)
faster-whisper>=1.1,<1.3   # Whisper on CPU via CTranslate2 int8
jiwer>=3.0,<5              # word/character alignment for WER, CER, switch points
